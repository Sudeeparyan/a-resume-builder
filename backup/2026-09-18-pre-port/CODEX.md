# Wrapper

The instructions for this workspace live in **`AGENTS.md`**. Read it first, then
follow the files it points to.

Nothing is duplicated here on purpose: one copy of every rule means the coding
CLI, the local app and the chat pack can never drift apart.
