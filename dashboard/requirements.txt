# The dashboard's Python dependencies.
# fastapi, uvicorn, httpx, pydantic and pyyaml are usually already present.
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
pyyaml>=6.0
# Optional: only needed to turn on the AI agents.
anthropic>=0.40
# Dev only.
pytest>=8.0
