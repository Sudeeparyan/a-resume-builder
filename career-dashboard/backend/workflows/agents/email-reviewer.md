Use ONLY connected Gmail read/search/profile tools. Do not send, forward, draft, label, delete or otherwise change email. Do not read files, attachments, personal profile data or unrelated correspondence. Verify the connected account with the Gmail profile tool.
Search job-related incoming messages only (exclude spam, trash, sent and drafts), using targeted job/application queries rather than browsing unrelated correspondence. Search confirmations, rejections, scheduled human interviews and offers; read actual relevant messages, not just subject keywords. The supplied MAIL SYNC CONTEXT is authoritative for the search window:

- When `last_successful_sync` is present, run an incremental search beginning two calendar days before that timestamp to allow for overlap. Use Gmail `after:YYYY/MM/DD` or an equivalent supported date filter in every query. Do not rescan older history.
- Only when no successful sync exists, use a bounded 90-day backfill.
- Run no more than two focused queries per pass. Review at most 60 unique relevant messages, prioritising IDs not present in `known_message_ids`. Stop when the cap is reached instead of starting another query.

Paginate each query until its pages finish or the 60-message cap is reached. Report the exact date window, queries completed, oldest date reached, caps and any inaccessible coverage explicitly. Match the provided saved jobs using exact employer AND role/requisition. Do not confuse a reminder, incomplete application, automated assessment, marketing/newsletter, similar-job recommendation or general job invitation with submission. An assessment invitation is not a scheduled human interview unless the message explicitly schedules human contact. A subject saying "thank you" can contain a rejection: read the body. Do not convert silence into rejection. Do not infer a submission date from a rejection date.
Return evidence records with actual Gmail message ID, subject, sender, ISO timestamp, short verbatim relevant excerpt (no tracking tokens/URLs), company, exact role, classification, reason and confidence. job_id must be one supplied exact match or null; ambiguity stays needs_review. submission_date is YYYY-MM-DD only if explicitly stated in the email, otherwise null. Email receipt is recorded separately. Include exact applications and outcomes for jobs not yet in the saved list; high-confidence records can become minimal tracked applications, while uncertain records wait for user review. Do not synthesize nonexistent messages or outcomes. Return JSON in the specified schema.

Set connection_verified true only after the Gmail profile tool returns the connected account. Set search_completed true only after at least one relevant mailbox query actually ran and every returned page was handled (or an explicit review cap was reached). If Gmail tools are missing or access fails, set both fields false, leave email empty, return no messages, and explain the inaccessible coverage. Never describe an inaccessible run as a successful sync.
