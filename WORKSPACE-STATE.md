# Career workspace state

Updated 25 September 2026 (the dashboard holds several people's profiles; Annie is the locked backup, profile 1).

## Where things stand

- **No AI marks in any resume or cover letter: the `remove-ai-marks` skill, built in (25 Sep), uncommitted.** The skill file that was dropped loose at `.agents/skills/SKILL.md` now lives at `.agents/skills/remove-ai-marks/` (with `agents/openai.yaml`) and is in the AGENTS.md skill table, `REQUIRED_SKILLS` and the resume-tailor/hunt skills. Its HTTP service (`127.0.0.1:8765`) is not on this PC, so its deterministic part is built into the app: `backend/ai_marks.py`. `career.tex_escape` (every value written into `resume.tex`), each Resume Studio save and each cover letter drop hidden characters (zero-width, direction controls, BOM, tag characters, variation selectors), make unusual spaces plain and fix Cyrillic/Greek look-alike letters inside Latin words. Every compiled PDF (`validate_resume.compile_latex`, Studio preview and fit) keeps only Title and Author; producer, creator, the "Evidence-grounded resume" subject, keywords, dates and XMP are removed. `career check-resume` fails a source or PDF that still has a mark (`qa.json` → `ai_marks`). The skill's Layer B (paraphrase) is never run on resumes: the wording must stay the registered wording. A scan of every existing `.tex` found no hidden characters. Tests: `tests/test_ai_marks.py` (5), two new validator mutations and a metadata check on the real Tectonic compile in `test_resume_contract.py`; `career check` passes. **Next:** nothing needed; older PDFs keep their old metadata until they are recompiled.

- **Accuracy upgrade: one verified requirement check per job, one engine for the 7 AM run, a phone-friendly Assistant, duplicates removed (24 Sep, night), uncommitted.**
  - *Job fit is a verified requirement matrix* (`backend/services/fit.py`, specialist `fit_analyst`, table `job_fit` via migration 8, `FIT_VERSION` fit-v3). The AI (free plans only: Kimi → Codex → Claude, never Azure; rules when none is free) lists what a posting asks for, each item quoted from it, with the registry ids that meet it; the code drops excerpts not in the posting and ids not in her registry, keeps coursework-only items at "partial", a never-claim skill at "missing" (only "partial" when it is one of several bracketed examples), and leaves work-permit wording to the sponsorship gate. The prompt leaves out personality lines and makes an "including X, Y, Z" list one item. Score = coverage 75 (must-haves 55, nice-to-haves 10, duties 10; two imaginary half-met items per category) + role/seniority 15 + location 10; saved only with no blocker, ≥ 65 and at least half the must-haves met. The rules fallback credits a field (ML, AI) from the tools that prove it (PyTorch, scikit-learn, computer vision…). It replaced the word-overlap score in discovery (the AI checks only a shortlist) and feeds tailoring (`job_tailor` gets the verified requirements; verified skills proving a met must-have are topped up, at most 4; a tailor that leads with another company's signature project or rewords a verified one is repaired, with one retry that gets the exact reason), resume coverage (v6), the study plan's gaps, research, the chat (`job_fit` tool) and a *Requirement check* panel in Studio → Check match (`GET/POST /api/v2/jobs/{id}/fit`). At server start `fit.backfill` gives saved jobs the rules check (no AI), replacing the old overlap scores (Color Health 94 → 77). Retired: the never-called posting graph (`requirement_extractor`, `relevance_judge`, `company_investigator`, `posting_verifier`).
  - *Fixed along the way (found by the live run):* "Alameda, CA — on-site" was read as outside the US (a work-arrangement note after the state now passes; Penumbra's Associate Data Engineer, fit 83, had been turned away); the word "requirements" was read as a missing business-analysis skill; Assurance said "76 still waiting" when 5 suggestions were (`pending` now counts suggestions only; registry items never need a decision).
  - *Calibration* (`backend/scripts/eval_fit.py`, read-only): her 4 saved jobs score 69–91 and pass; the one she removed as unsuitable (Marsh, AI Engineer) scores 59 and fails.
  - *The 7 AM run is the Daily Search engine* (`daily-job-search/morning_run.py`): `POST /pipeline/run` with every helper on, company list first (also preparing saved jobs left unprepared: `include_unprepared`), then the balanced web mix; the report is built from each job's steps; morning resumes now get company research; `--base-url` / `CAREER_BASE_URL` points it at a test copy. Daily Search follows the AI in Settings and no longer sends or remembers one.
  - *Assistant page:* no right-hand rail; the chat is centred (max 860 px) with a sticky one-row bar (AI chip → Settings, "N running" → Agents, New chat, History, ⋯ with Search, Download and **Auto-apply changes**), four starter chips above the box (one sideways row on phones), a short placeholder, keyboard hints on desktop only (the `.chat-hint` cascade bug is fixed), a "1 failed" mark on *What I did*; on phones no avatars, 85 % bubbles, a stacked before/after, full-width buttons, 40 px tap targets and a labelled bottom bar (Chat, Dashboard, Search, Resumes, More → Assurance, Profile, Agents, Settings).
  - *Removed duplicates (her choice):* Studio's own chat box (`AgentControl.tsx`, `/instructions`, `instruction_tracker.py`, the `instruction_interpret` run; Studio links to the chat, whose `edit_resume` shows a before/after and waits for her yes), Studio's second research panel (`resume_advisor`; *Analyse role* stays), Daily Search's goal cards and plan editor (now **Your week** + **Edit plan** on the Dashboard; Daily Search shows one line), and Daily Search's AI picker and "Allow N a day" (a status line linking to Settings). The independent PDF review moved into Studio → Check match.
  - **Verified:** full backend suite 546 passed, 7 skipped, none failing (new `tests/test_fit.py` 13, `tests/test_morning_run.py` 3, tailoring and pipeline tests); 74 frontend tests; production build; `career check` passes. Live on a disposable copy (port 8011, paid limit 0, scheduled tasks off): the company-list search read 1,249 postings, the hard gates left one, and the AI check on Kimi rejected it (a web/CMS role, 0 of 5 must-haves met); the morning run finished in 20 min with 0 problems (Kimi hit its 15-min web limit and Auto moved the search to Codex; no Azure call); an AI requirement check on Color Health took ~70 s on Kimi with every excerpt found in the posting; tailoring on Kimi took 2 min 50 s and gave one US Letter page (99 % full) with 5 suggestions to review. Screenshots at 375/414/768/1024/1280/1536 px: no sideways scroll anywhere.
  - **Unresolved / next:** the dashboard on 8010 runs older code; close its window and double-click **Start Dashboard.cmd** (the start-up refresh then re-scores her saved jobs). A balanced web search on Kimi can exceed Kimi's 15-minute web limit before Auto moves on (20 min for this morning run). Nothing is committed.
- **Auto: one AI route across her plans, a paid-only daily limit, plan-usage estimates, and one AGENTS.md for every AI app (24 Sep, evening), uncommitted.**
  - *Auto (the new default AI)* works like OpenRouter across her own plans: **Kimi Code K3 → Codex (GPT-6-Astra writes, GPT-6-Luna reads) → Claude Code (Opus writes, Sonnet reads) → Azure OpenAI (paid) last** (`backend/ai/router.py`). A usage-limit error rests that plan until the reset time it printed (`backend/ai/limits.py`; an hour if it says nothing), any other failure moves to the next plan, three failures in a row rest a plan 15 min, and Azure is used only when all three are resting or failing. Rest state and usage windows are PC-wide in `career-dashboard/.ai-plan-health.json` (git-ignored). Codex now recognises its own "usage limit" message; Claude and Kimi keep the reset time. The gateway, the specialist team, Daily Search, the chat and the morning run all use it; the trace shows "Auto → Codex · …" and each switch is a `provider_fallback` event.
  - *Daily limit, re-based:* `ai_policy.daily_call_limit` now counts **paid calls only** (Azure, API keys; 0 = never pay; up to 200). Free plan calls never count. When it is used up, Auto skips Azure and keeps working on the free plans.
  - *Plan-usage estimates:* every free-plan call is metered against that plan's 5-hour window (real tokens from Claude, an estimate from the text for Kimi and Codex). Each plan's limit is what she types in Settings, else learned the moment the plan runs out, else a starting guess (Kimi 1M, Codex 1M, Claude 500K tokens). Settings shows "% of its 5-hour limit used"; Daily Search shows, before Start, how the search's tokens fill each plan ("Kimi 30% → 52%", "Azure: not needed").
  - *Settings:* an Auto tile first, a route editor (order, on/off, per-plan limit, **Try now** to clear a rest), "Paid AI calls a day". `workspace.py ai-status` / `ai-wake --provider X` do the same from any AI app.
  - *Annie's saved choice was switched to Auto* (Settings and Daily Search; logged as `ai_main_provider_chosen`). Before: Azure `gpt-6-luna` everywhere, but **this workspace has no `AZURE_OPENAI_API_KEY`** (endpoint and deployment are set), so Azure shows "not set up" and Auto skips it until a key is added in Settings → API keys.
  - *One AGENTS.md for every AI app:* the root `AGENTS.md` is the single rulebook (Claude Code 2.1.277+ reads it when no `CLAUDE.md` exists; this PC has 2.1.281). `CLAUDE.md`, `career-dashboard/CLAUDE.md`, `.claude/` and `career-dashboard/.github/agents/` are gone; their content is merged into `AGENTS.md` and the six skills in **`.agents/skills/`** (hunt, job-hunter, resume-tailor + references, profile-intake, interview-prep, verify-job-url with its script), which Codex and Kimi Code load directly. The three Copilot workflow files moved to `career-dashboard/backend/workflows/`. New launcher `career.cmd` / `career` (`career help`): the same commands on Windows and Mac for every AI app, one database. `career-dashboard/AGENTS.md` stays as Annie's policy (the app reads it). `validate_workspace.py` now checks the skills and warns if a `CLAUDE.md` or `.claude/` comes back. The moves are staged in git (not committed). `/hunt` is no longer a Claude slash command: say "give me 10 companies" or "hunt 10" in any app.
  - **Verified:** full backend suite 527 passed, 6 skipped, none failing; 24 new router tests (`tests/test_ai_router.py`), updated `test_agent_control.py` and `test_verify_job_url.py`; 69 frontend tests (4 new plan-use tests); production build; `career check` passes. Live on this PC: a ping through the route was answered by Kimi K3 (9 s); with Kimi marked resting, Codex GPT-6-Luna answered (5 s).
  - **Unresolved / next:** the dashboard on 8010 still runs older code (started by hand): close its window and double-click **Start Dashboard.cmd** to get Auto; until then it cannot run AI steps with the new setting. Add the Azure key if Azure should be the last resort. The plan limits are guesses until each plan runs out once (or she types them in Settings).

- **Final end-to-end test on Azure, Codex and Kimi, and new profiles are set up in a chat (24 Sep, afternoon), uncommitted.** No Claude was used anywhere (checked in `ai_calls` / `agent_runs`).
  - *This PC first:* `backend/.venv` pointed at `C:\Users\sudeep\…` (the old machine) and could not start, so `Start Dashboard.cmd` and `Check Workspace.cmd` failed at their first step; it was rebuilt with this PC's Python 3.12. The dashboard now on 8010 was started by hand with the system Python (`python run.py`) and runs the older code: close its window and double-click **Start Dashboard.cmd**. The Windows task "Annie Daily Job Search" runs `C:\Users\sudee\Desktop\annie-resume\a-resume-builder\a-resume-builder\daily-job-search\morning-run.cmd`, a *different copy* of the workspace, not this one (left unchanged). Kimi Code's terminal app is now installed and signed in here; Codex is found in `AppData\Local\OpenAI\Codex\bin`.
  - *The live run (disposable copy on port 8011):* Azure `gpt-6-luna` searched the web for 2 jobs (70 s). Johns Hopkins, Data Engineer I, was wrongly excluded by the AI's own note ("No visa-sponsorship … sentence was visible"); after the fix it was restored and researched on Azure. A second web pass honestly found nothing verifiable. The company-list search (no AI) found Databricks, Software Engineer, Web Products, which Azure researched. **Job 1 (Johns Hopkins):** tailored on Codex (52 profile items + 2 suggestions, one full page, ATS readiness 95) with a Codex study plan. **Job 2 (Databricks):** tailored on Kimi (140 s; 54 + 6, one page at 97.6%, ATS 95) with a Kimi study plan. Connection tests: Azure 4 s, Codex 11 s, Kimi 12 s.
  - *Fixes the run exposed:* (1) the gate now has built-in absence-note guards for every profile (`sponsorship.py ABSENCE_CLAIMS`), and anything after "but/however/although" is still screened, so a real refusal after an absence note still excludes; (2) "not eligible for (visa) sponsorship" was not excluded anywhere; added to Annie's `data/config/sponsorship.yml` and both country packs; (3) research started from the Agents tab, the assistant or the morning run now saves `company-research.md` (only Daily Search did, so the tailor never saw it); (4) a restored posting keeps its real tier (Johns Hopkins came back as C; a .edu employer is S); (5) one rule-breaking *suggested* item (Kimi proposed publication wording) no longer throws away the whole tailoring: that item is left out with a warning and the rest is saved (verified items stay strict); (6) Settings' Codex "Test connection" now makes a real call (it always said OK); (7) Claude Code, the Kimi runner and the Tectonic compile read and write UTF-8 (Windows' cp1252 default could fail on "→" or "Ł"); (8) five tests hard-coded the resume revision `2026-09-18.1` and now read it from `evidence.yml` (the long-standing "5 known failures"); (9) `validate_workspace.py` judged the base PDF stale by file time after a copy; it now trusts the hashes in `qa.json`; (10) user-facing text said "this Mac" on Windows.
  - *Worth knowing:* with 7 companies prepared, Annie's strong data projects are all taken as other companies' signature projects (one per company, by rule), so Johns Hopkins led with the FPGA project and its study plan carries a build-now spec.
  - **New: set up a profile in the chat.** An onboarding profile's page is now a chat (`frontend/src/features/OnboardingChat.tsx`; the form is one click away, "Use the form instead"). Attach Word/PDF/text files or paste text → "Read them now?" → reading shown live → what was found → questions **one at a time**, with numbered answers to click (or press 1–9), multi-select where several can be true, a line to type your own, and Skip: first the essentials (name if missing, country, roles, **where to work**, right to work, sponsorship, email, phone), then up to five from the new `intake_interviewer` specialist (the documents' open questions, then preferences). Typing a correction ("Also include Limerick") updates the fields; "also/add" adds to a list rather than replacing it. → "Build my workspace" → the full app opens. Answers are applied to the draft at once and written, in the person's words, to `QUESTIONS-FOR-YOU.md` (answered) and `09-anything-else.md`; they survive a re-read and a server restart. Backend: `backend/services/intake/interview.py` (`SetupChat`), routes `/api/profiles/{id}/intake/chat` (+ `/answer`, `/files`) in `intake/api.py`, docs in `docs/API.md`. The built profile's Assistant uses the same answer card (`components/QuestionCard.tsx`) for its own questions; a suggested answer that ends in "…" fills the message box instead of sending.
  - *Tested live on the copy* with `example_profile/About Me-1.docx` on Azure: read in 108 s (461 of 461 blocks, Ireland, Stamp 1G, 8 open questions); interviewer questions took 4–9 s each; build ~2 s; base resume one A4 page (automated pass, visual review pending). The profile page showed only Srikanth's data; Annie's 7 jobs and 39 exclusions were untouched. Pages checked in headless Edge at 1280 px and a true 400 px (no sideways scroll). Fixed during this test: block ids ("P329") in questions, a missing "where to work" question, "also include" replacing a list, and a doubled "built" message that could reload the page too early.
  - **Verified:** 503 backend tests pass, 6 skipped, none failing (the "5 known failures" are gone); 65 frontend tests (6 new); the production build, `validate_workspace.py` and `check_layout.py`. New tests: `tests/test_setup_chat.py` (7), plus additions in `test_sponsorship.py`, `test_excluded_api.py`, `test_tailoring.py`, `test_pipeline_run.py`, `test_model_choices.py`, `test_kimi_cli.py`, `test_claude_code.py`, `frontend/src/components/QuestionCard.test.tsx`.
  - **Unresolved:** restart the dashboard on 8010 (above); decide whether the Windows morning task should point at this workspace; SimpliSafe is still excluded in Annie's real database (restore it from Dashboard → Excluded roles); Srikanth's real profile is still not created here (the tests used a disposable copy); everything is uncommitted.

- **Multi-profile dashboard (24 Sep), uncommitted.** One server now serves every profile, each at its own address: Annie at `/p/annie/`, a new profile at `/p/<id>/`. Every profile gets its own app: files, database, gates, agents, chats, AI settings and budget, and daily search. A request reaches a profile only through its address, so no data mixes. **Annie is untouched:** her folders, database, prompts and US rules are unchanged, and she is locked (the API refuses her Reset and Delete). New profiles live in `career-dashboard/profiles/<id>/` (kept out of git; registry in `profiles/registry.json`).
  - *Building a profile from documents:* profile menu → **New profile** → drop Word, PDF, text or Markdown files → **Read my documents**. Every paragraph and table row is numbered, the AI reads the documents section by section (an extractor plus a completeness auditor per section), and a coverage ledger shows that every block is cited, background, or kept word for word in `09-anything-else.md`. The review card shows what was found, the questions still open, the country and right to work, and fields for missing essentials. **Build my workspace** writes the whole file set in one step: profile, evidence registry, context files 01–09, story bank, the country's gate, portals, discovery guide, a per-profile `AGENTS.md` and an evidence-tagged base resume. The resume is compiled and checked, and re-rendered shorter if it runs past one page. AI choices are copied read-only from Annie's settings; Gmail never is.
  - *Country packs* (`backend/countries/us`, `backend/countries/ie`) hold location matching, the work-permit gate, paper, spelling, time zone and job-board wording. Ireland: A4 one page, Irish/UK spelling, Europe/Dublin, excludes "EU/EEA citizens only", "Stamp 4/5 required" and permit refusals, and never excludes "must be eligible to work in Ireland". There is no sponsor index, so the tiers are A/C and the balanced mix needs no sponsor record. Pages, agents, tools and prompts speak each profile's country; Annie's text is unchanged.
  - *Reset* (back to an empty onboarding page) and *Delete* are in Settings → Profile. Both show "This process cannot be reverted" and need the exact profile name typed. They stop the profile's work, remove its folder and remove its scheduled task.
  - *Daily search per profile:* building a profile registers its own Windows task, "Career Daily Job Search - <name> (<id>)", at 07:20, then 07:40 and so on (Annie's 07:00 task is unchanged). It can be switched off in Settings → Profile. `morning_run.py --profile <id>` writes to `profiles/<id>/daily-job-search/`.
  - *Gmail stays Annie's:* this PC's Codex Gmail connection is her mailbox, so every other profile refuses Gmail sync and its Dashboard says why.
  - *Tested end to end on a disposable copy* with `example_profile/About Me-1.docx` (Srikanth Nadesharam, Cork, Stamp 1G until DEC2027). The real AI read it in about 2 minutes (24 calls, 12 sections): 461 of 461 blocks accounted for, the `ie` pack and the permit detected, 7 open questions. The build finished in about 2 s; the base resume passed as one A4 page. Profile 2's API showed only his data. An Irish posting saved with fit 92; "EU/EEA citizens only" was excluded with its sentence; an Austin, TX posting scored 0 for location; Annie's summary and excluded list were byte-identical before and after. Reset (wrong name refused, then back to onboarding) and rebuild were checked, and Annie's Reset/Delete were refused. Pages were checked in headless Edge at 1280 px and a true 400 px (no horizontal overflow). A live 2-job Daily Search for profile 2 on Azure OpenAI `gpt-5.1` took about 70 s. It returned only Irish leads: Accenture's AI & Data Graduate Programme (Dublin) and Ardonagh Analytics Lab (Mullingar, held for a legitimacy review). Cork roles asking for 5+ years or titled Senior were turned away; no US posting appeared. (`gpt-6-luna` was rate-limited, HTTP 429; a second `gpt-5.1` pass got empty web results.)
  - *Fixes this testing exposed:* the review card asked questions another section already answered and treated "2023" vs "August 2023" as a conflict (both fixed); resume lines now lead with measured results and never repeat a number; skills are printed once; the Agents page, Daily Search, Resume Studio, tier tooltips and chat tools no longer show H-1B, US Letter, "US postings" or US Central time on an Irish profile. A Windows race let the onboarding page flip back to "uploaded" mid-analysis while the state file was rewritten; it is fixed, with a test that reproduces it on the old code. The profile list could have been rewritten with only Annie if one read failed; it now retries and refuses instead. The live search exposed a gate bug: Accenture was excluded because the AI's own note ("the pages … do not show the precise sentence … for example, whether applicants must have the right to work in Ireland 'now and in future'") matched a refusal pattern. The Ireland pack now has absence-claim guards for that wording; real refusals still exclude, and a test uses the exact sentence. A restored posting on a non-US profile no longer shows the H-1B tier label. Settings no longer lists a Gmail route for non-Annie profiles.
  - **Verified:** 478 backend tests pass, 5 skipped (23 new in `tests/test_intake_build.py` and `tests/test_profiles.py`). 5 fail exactly as on a clean checkout of HEAD: their fixtures hard-code `C:\Users\sudeep\Desktop\...` (`test_batch_quality` ×3, `test_profile_sync` experience edit, `test_resume_quality` tailored fixture). Also: 59 frontend tests, the production build, and `validate_workspace.py`.
  - **Unresolved:** (1) Annie's database has an active non-US job, **Marsh — AI Engineer, Dublin, County Dublin** (irishjobs.ie, id `4f01574f63c33c0b`, created 23 Sep, prepared). It predates this work and was not touched; remove it from her Dashboard if it should not be there. (2) Srikanth's real profile has not been created in this workspace; the tests used a disposable copy. (3) Annie's own `data/config/sponsorship.yml` has the same absence-claim gap (it guards "does not state whether", not "do not show the precise sentence…"). It was left unchanged as promised; adding the same three guards there is a one-line-each change if wanted. (4) Everything is uncommitted.

- **Codex and Azure tested end to end; the assistant is faster and reachable from every tab (23 Sep, evening), uncommitted.** Claude and Kimi were deliberately left out of this round. Azure has no `gpt-6-sol` deployment (HTTP 404; the resource has `gpt-6-luna`, `gpt-5.1`, `gpt-5.1-eu`), so Azure runs used **`gpt-6-luna`**; creating `gpt-6-sol` in Azure AI Foundry and adding it to `AZURE_OPENAI_DEPLOYMENT` in `.env` is all it takes to offer it. **Result:** a full Azure pipeline run saved **Color Health — Software Engineer, New Grad 2026** (tier B, fit 94) with research, study plan and a one-page PDF; Codex discovery and drafts (Vestmark, Perpay, HexArmor) worked earlier until the ChatGPT plan's usage limit. Later Azure searches found real leads that the rules correctly turned away (graduation windows after May 2026, sponsorship refusals, a CareerBuilder repost, a "Quality Engineer" title outside `profile.yml`'s targets).
  - *Search fixes:* the legitimacy check now accepts the federal USCIS H-1B employer list (already in `data/sponsors`) as proof of legal presence, so Notion, Torc, Applied Intuition and the like are no longer dropped for lack of a registry page, and the guide tells the AI to return such postings; the AI's own `excluded` notes (Azure fills every field) go to *Rejected leads* instead of being shown as sponsorship-gate verdicts; the gate now catches "unable to provide visa sponsorship"; Greenhouse/Lever/Ashby postings are read in full from the board's public feed before every check; a broken web-search tool says so instead of "found 0 jobs"; Azure rate limits (250,000 tokens/min on `gpt-6-luna`) now wait 15/30/45/60 s instead of retrying within seconds.
  - *Resume fixes:* a tailoring that kept few skills no longer prints an empty "Data Engineering:" line or sentences as skills (each line keeps at least half of its base entries; predicted skills are 1–4-word names; skills are filed by the base template's categories, so re-tailoring never drifts); the tailor may write up to 16,000 tokens (a reasoning model used 4,018 of 4,096 and a longer plan was cut off); evidence comments are rebuilt on each tailoring, so Assurance no longer shows a false "Needs evidence"; JD section headings ("Helpful Though Not Required:") are no longer scored as requirements (scoring `career-assessment-v5`; cached requirement lists refresh per version, and re-scoring after a version change no longer fails with a server error); claim text shows "C#" and no LaTeX line breaks. The Color Health resume was re-tailored on Azure and re-scored (coverage 83, ATS 95, one page, all skill lines filled).
  - *Assistant:* one decision can run up to five read-only lookups (`more_calls`), and the snapshot now says per job whether a resume PDF, research and a study plan exist and how Daily Search is doing, so "which jobs still need a study plan?" is answered in about 6 s with no tool call (was 44 s and five model turns). New tools: run, follow and stop the **Daily Search pipeline**, the last **search report** (what was turned away and why) and the **Assurance** check. If the chat's AI fails and Settings names a *Backup provider*, the chat retries there and says so. A reply about several jobs shows one row per job instead of the last job's card. Every other tab (Dashboard, Daily Search, Resume Studio, Assurance, Profile, job details) has an **Ask the assistant** menu with questions about what is on screen: read-only ones are sent at once, ones that start work open the chat ready to send. Verified live on Azure `gpt-6-luna` through the real page in headless Edge.
  - *Other fixes:* the Assurance tab no longer crashes when a tailoring proposed more projects than the page holds; Windows encoding: app files (`QUESTIONS-FOR-YOU.md`, `profile.yml`, `portals.yml`, resume `.tex` builds, the history CSV) are read and written as UTF-8 (the dashboard showed garbled curly quotes; an AI-written "→" could have broken a PDF build).
  - *Settings put back after testing:* daily AI call limit 6, Daily Search defaults (Claude Code · Sonnet, balanced mix, 2 jobs), main AI back to the built-in default (the `ai_preferences` row the test created was removed). Testing used 36 calls today, so today's allowance is spent until *Allow N a day* is clicked. The test chat stays in the Assistant history (delete it there if unwanted).
  - **Verified:** 460 backend tests pass (5 skipped; 20 new), 58 frontend tests (2 new), the production build, and the workspace and layout validators.
  - **Unresolved:** SimpliSafe (Excluded roles, record `ccfb7dd8f85607b3`) was excluded by an earlier gate bug and should be restored from Dashboard → Excluded roles; the Vestmark, Perpay and HexArmor drafts were not re-tailored after the skills fix; Kimi and Claude were not tested this round, and `claude_code.py:175` and the Kimi runner's stderr still decode with the Windows default (flagged, not changed).

- **Tectonic installed and PDF detection fixed (23 Sep).** Installed official Tectonic 0.17.0 for Windows at `C:\Users\sudeep\.local\bin\tectonic.exe` after verifying its SHA-256. The dashboard now finds a compiler added after it started, and Daily Search refreshes the PDF switch every 15 seconds. The base resume was rebuilt, visually reviewed, and passed as one US Letter page. The live pipeline API reports `tools.pdf: true`. Verification: 424 backend tests passed (5 skipped), workspace and layout validators passed, frontend build passed, and 56 frontend tests passed. The second full check stopped at the previously stale base PDF after all backend tests passed; it was rebuilt and the remaining checks passed separately.

- **Model choices for every AI, and Azure searches the web (23 Sep, late afternoon), uncommitted.** Daily Search's *Which AI* step now offers a model for each app, like Claude's Sonnet/Opus/Haiku: Codex shows *Default* plus the first three models her ChatGPT plan lists (GPT-6-Astra, GPT-6-Sol, GPT-6-Luna, read from `~/.codex/models_cache.json`, passed as `-m`); Kimi Code shows *Default* (K2.8 Preview) plus K2.7 Code Highspeed, K3 and K3-256k from `~/.kimi-code/config.toml` (passed as `--model`); Azure shows every deployment on the resource (`gpt-6-luna`, `gpt-5.1`, `gpt-5.1-eu`, listed live and cached a day). **Azure now does web work itself** (finding jobs, company research) through the Responses API's `web_search` tool, verified live with the real schemas: research 19 s with 7 sources, a 2-job search in 30 s with real posting URLs; all three deployments answer. Codex `-m gpt-6-luna` verified live. The summary bar's AI-call tile now reads "Daily limit: X of Y left", and when a search needs more calls than are left it says why ("1 to find jobs, 15 for Company research and 5 for Study plan"), what an AI call is, and offers *Allow N a day*. **Kimi Code is still not usable here:** what is installed is the VS Code extension (0.8.1), which runs Kimi inside the editor and ships no `kimi` program; the sign-in and models are there. Installing the terminal app (`irm https://code.kimi.com/kimi-code/install.ps1 | iex` in PowerShell) shares that sign-in; the Kimi card and the start window give this command. The installer was reviewed (checksummed download to `~/.kimi-code/bin/kimi.exe`, user PATH only) but not run: the session's permission rules block running downloaded code. New `tests/conftest.py` gives every test empty Codex/Kimi homes so results never depend on this PC's apps; new `tests/test_model_choices.py` (7) and 5 Azure tests.
- **Daily Search is a pipeline you set up (23 Sep, afternoon), uncommitted.** The old "Find suitable jobs" button and preset dropdown are replaced by a four-question card: *how many jobs* (1–15, still capped by what is left in today's plan), *where to look* (web search, balanced mix, or her company list with no AI), *which AI* (Claude Code · Sonnet/Opus/Haiku, Codex, Kimi Code, and any keyed API; ones not on the PC are shown greyed with the reason) and *which helpers run for each job*, as on/off switches (Company research, Tailor resume, Study plan, One-page PDF; quick picks Recommended / Everything / Just resumes / Just find jobs). A summary bar shows time ("About 39 min, done around 2:06 PM"), tokens and AI calls against today's app limit before **Start search**, with a one-click raise when a run would exceed the limit. While it runs, a progress card shows each job's helpers, time left and *Stop after this step*. Estimates start from first-run numbers and learn per AI from each finished run (step times; tailor tokens are measured). Backend: `backend/services/pipeline.py` (routes `/api/v2/pipeline*`, table `pipeline_runs`); discovery takes a `count` (the balanced mix scales its 2/1/2 split); research is now saved to `company-research.md`, which the tailor reads. Nothing is submitted; every gate and the Assurance review still apply. The morning run is unchanged (5 jobs, tailor + study plan + PDF).
- **The app now finds Claude Code and Codex on this PC.** Both were installed (the VS Code extension's `claude.exe`, the Codex app's `codex.exe`) but invisible on Windows, so everything silently fell back. Kimi Code is not installed here (no binary, no sign-in), and the page says so.
- **Azure OpenAI works: `gpt-6-luna` on `gptaitest.openai.azure.com` (verified live).** New provider `azure_openai`: `AZURE_OPENAI_API_KEY`, `AZURE_OPENAI_ENDPOINT` and `AZURE_OPENAI_DEPLOYMENT` in `career-dashboard/.env`. The model needs the Responses API and no temperature; both are set. An Azure-format key pasted as `OPENAI_API_KEY` is filed as Azure (otherwise OpenAI looked "ready" and every run would have failed against api.openai.com). (Superseded late afternoon: Azure now searches the web itself; see the top entry.)
- **Start Dashboard.cmd is the one start command.** It sets up on first run, prints which AI apps and whether the PDF compiler were found, and restarts a copy running older code (never while a search or agent run is busy); `--restart` forces that. The copy on 8010 right now predates this, so close its window and double-click the start file once.
- **Verified:** 403 backend tests pass (12 skipped), plus 11 new pipeline, 3 Azure and 3 launcher tests; 56 frontend tests. The real page was driven over the DevTools protocol on a disposable copy at 1280 px and a true 400 px (no horizontal overflow; the old page overflowed to 488 px). A live pipeline run on that copy (company list, 1 job, tailor on Azure `gpt-6-luna`) finished in 93 s: Snowflake "Software Engineer – Openflow", 9 profile items + 5 suggestions, 6.9K tokens. **Resolved 23 Sep:** Tectonic is installed; the One-page PDF step and page fit have a working compiler, and the base resume PDF has been rebuilt.

- **A Profile save now updates everything (23 Sep), uncommitted.** Before, saving on the Profile page changed only `career.db`; the resume builder, the validators, job screening and the Claude Code / Copilot agents read `profile.yml`, `evidence.yml` and the fixed resume template, so an edit never reached a resume. Now one save does everything in one database transaction. It writes the knowledge row (registered at once, so no *Confirm* step for her own edits). It rewrites that entry's lines in `data/config/profile.yml` or `data/context/evidence.yml`, with a dated `Profile page (dashboard) > edited …` source ref; only that block changes, and the file is re-read and compared before saving. It keeps the linked twin in step (e.g. profile.yml's phone and `CONTACT-PHONE-001`). It updates the base resume template (headings, bullets, skills lines, project slots, header) and the drafts of jobs still *saved*/*prepared*, as a new Studio version. It bumps `candidate_revision` in both files, so older PDFs show as needing a rebuild. Drafts of jobs already applied to are never touched, and wording customised in Studio is left alone. A failed write puts every file back. Removal keeps the entry on `hold` with `profile_removed`; entries every resume prints (name, contacts, InsOps, Soliton PE, MS degree, target roles, …) can be edited but not removed. Suggestions from the chat, the assistant, Studio or the CLI still wait for *Confirm* (with a new *Keep it* for suggested removals), then go the same way. Refused with a plain message: a skill on the never-claim list, or an edit that breaks a registered project; a new project that is only a sentence is kept on the profile and off resumes, and the card says why. The toast says where a save went ("Also updated: profile settings, evidence registry and base resume"). Code: `backend/services/profile_sync.py` (files, twins, drafts), `backend/services/resume_sync.py` (LaTeX); no agent, AI provider or chat code changed. Verified: 8 new tests in `tests/test_profile_sync.py`, which run the workspace validator and `validate_resume.py` on the changed copy; a smoke run of seven kinds of change; and the real page driven in headless Edge on a disposable copy (role edit, phone, locked Remove, skill-group removal; no browser errors). Three older tests that asserted "Confirm never touches `evidence.yml`" were updated to the new behaviour. **387 backend and 43 frontend tests pass.** Later, with a parallel session's model-choice work and its new `backend/pdf_compiler.py` in the tree, the test workspace fixture (`tests/test_career_workspace.py`) also copies `pdf_compiler.py` (without it, 17 tests failed with ModuleNotFoundError); the combined suite passes, 424 passed and 5 skipped. Documented in `docs/API.md` (Profile) and `DATA_CONTRACT.md`. **Unresolved:** restart the dashboard on 8010 before using Profile. `AGENTS.md` still says the numbered context files "win any disagreement"; a Profile edit newer than them is marked by its dated source ref, but that policy line was left for Annie to decide on (agent instructions were out of scope). The stale base-resume PDF and projections noted below still stand; this machine has no `tectonic`, so PDFs can't be rebuilt here.

- **Profile page is readable and form-driven (23 Sep), uncommitted.** The Profile tab no longer shows raw JSON anywhere (entry details, Target roles / Location preferences, the chat preview, `profile.yml` and the evidence registry under Advanced). It opens with her name, status and contact links, then Basics (grouped: contact, work authorization, career snapshot, how resumes show these), Job search (roles and places as tags), Experience (newest first), Projects, Skills (grouped by her own tiers: Strong / Used it / Touched it / Honest gaps), Education, Certifications and Other facts; imported ID titles read as names ("Cloud", "Publication", "GitHub"), and each entry's registry notes sit under *About this entry*. Add/Edit is now a form per kind (role, employer, dates, location, one line per bullet; tags for technologies and skills), and pending edits can be confirmed one by one. The link is one module, `backend/services/profile_fields.py`: it defines the forms, reads the fields from a `knowledge` row and writes them back into the registry's own `data` keys with a matching `summary`, so what the page shows is what the database holds and what agents read. Saves without form fields (Profile chat, assistant, CLI) are unchanged, and no agent, AI provider or chat code was touched; untouched rows are not rewritten, so AI caches stay valid. Verified by driving the real UI on a disposable copy (edit → save → DB row and `active-profile.json` agree; lists edit for Target roles; add and remove a certification; confirm all) at desktop and phone width. **379 backend tests (8 new in `tests/test_profile_fields.py`) and 41 frontend tests (10 new in `Profile.test.tsx`) pass**; `docs/API.md` has a Profile section. **Unresolved:** the dashboard on 8010 was started before this change, so restart it before opening Profile (until then the page asks for a restart instead of loading). `validate_workspace.py` and `check_layout.py` report a stale base-resume PDF and stale `jobs.json`/`activity.json`; both predate this work (this clone's gitignored `career.db` was created fresh at 10:30 UTC today with no jobs, while the committed projections came from another machine's database).
- **Scheduled morning run live (23 Sep), uncommitted.** The daily search no longer needs a manual start: the Windows task **"Annie Daily Job Search"** (installed by `daily-job-search/Install-MorningTask.ps1`, daily 07:00, catches up at the next logon/wake if missed) runs `daily-job-search/morning_run.py`, which starts the dashboard on 8010 if it is down, ages quiet applications, runs discovery (tracked portals + one AI `balanced_five` pass, capped at 5 jobs a day), then per new job: Resume Studio AI tailor, study plan, and a compiled/validated one-page PDF. It regenerates the projections and writes a plain-English `daily-job-search/<date>/MORNING-REPORT.md`; logs in `daily-job-search/logs/`. Verified end-to-end for real: a manual run plus a live scheduled-trigger run (7.5 min, 0 problems); an earlier rehearsal tailored 5 jobs (3 tailored with predicted items, 1 refused by the tailor's own guardrail, 1 hit the Kimi usage limit — all degraded gracefully to the deterministic one-page draft, which always exists). Three fixes the rehearsals exposed: (1) `atomic_write` in `backend/scripts/career.py` now retries the temp-file rename — a transient Windows lock (WinError 5) had failed a whole portals run; (2) the runner's logging is cp1252-safe for the scheduler's console; (3) validation in the morning run reads `qa.json` — a compiled one-page PDF counts as ready even while evidence-map/visual review stay pending (those are Annie's Assurance steps). The app's daily AI call budget was raised 6 → **15** (`ai_policy` pref) so a full morning (1 discovery + 5 tailors + 5 study plans ≈ 11 calls) fits; lower it in Agents → Advanced if wanted. Also new: `Check Workspace.cmd`, the Windows twin of `Check Workspace.command`. Nothing is ever submitted — Annie reviews Assurance and applies herself.
- **Kimi Code CLI wired in and verified live (22 Sep), uncommitted.** The signed-in Kimi Code app on this PC is now a third local runtime provider (`kimi_cli`, `backend/ai/kimi_cli.py`) next to Claude Code and Codex — no API key, work runs on the Kimi membership's own usage allowance. Settings lists it, Test connection proves it, and it can be chosen as the main AI (it currently is). Verified live against the real membership: a discovery run completed in ~7 minutes with honest bounded research (6 web searches, 12 page fetches) and surfaced one verified, eligible posting — **Shirley Ryan AbilityLab, "Engineer I, Machine Learning Ops – CBM Lab"** (Chicago; a cap-exempt nonprofit research hospital) — and Resume Studio then tailored the resume on the same runtime: 19 verified items plus 8 predicted ones (2 MLOps-aligned projects and 6 skills), still exactly one US Letter page, and the Assurance tab's keep/remove switches were exercised for real (one predicted skill kept, one removed; the draft repaired itself to revision 4 and stayed one page). Two genuine bugs the live run exposed were fixed: (1) the sponsorship gate excluded the Shirley Ryan posting because the researcher's own sentence *describing the absence* of restriction wording tripped the ITAR pattern — absence-claim sentences are now listed in `negation_guards` with regression tests, and the wrongly excluded job was restored; (2) the tailor used to fail closed because the model was never told the never-claim list, so its first proposal was discarded wholesale — the list is now in the prompt. One Windows-specific fix underneath: the Kimi CLI can lose buffered output on long runs when stdout is a pipe, so output is captured to a file instead. A second Windows fix: `check_layout.py` read the JSON projections with the machine's default codepage while the app writes them as UTF-8, so the first non-ASCII character in a projection (an em-dash in the discovery notes) produced false "stale projection" failures — the reads now say UTF-8 explicitly, and the layout check passes. Note: the membership has a rolling 5-hour usage limit; a run that hits it fails with a clear "usage limit" error and simply needs a retry later. **377 backend tests, 31 frontend tests pass; workspace validation passes.** The restored Shirley Ryan job shows no fit score (restored jobs skip the ranking pass until the next discovery run); everything else behaves normally.
- **Big upgrade merged (21 Sep), uncommitted.** Six phases, all tested: (1) fixes across the app (date labels, download links, modal backdrop, page error boundary, silent scheduler failures now logged, failed portal fetches reported with reasons, per-provider usage in the budget); (2) **Kimi** is a first-class provider next to Claude Code and Codex — Settings has a *Backup provider* card, and if the main provider fails a call the work moves to the backup automatically (named `MOONSHOT_API_KEY=…` keys only; a bare `sk-` token is never guessed as Kimi, and Kimi never gets web-search work routed to it); (3) the Assistant now shows a before/after **diff** for every change it wants to make, a collapsible **What I did** trace on every reply, and an **Auto-apply changes** toggle per chat (off by default — it still waits for her yes unless she switches it on); (4) the job-finder pipeline: every search returns **exactly 5 jobs, ranked best-fit-first**, each with a fit score and a one-line reason on the job card; per job, **Tailor** in Resume Studio rewrites only the Projects and Skills sections — her real registry content plus company-aligned *predicted* items — and the new **Assurance tab** lists every claim with an evidence score and a keep/remove switch (guardrails warn there instead of blocking; experience, education and personal details still hard-fail); statuses read as pipeline stages (Discovered → Tailored → Applied → Interview); (5) simpler pages: Profile is one top-to-bottom flow (Basics → Experience → Projects → Skills → Education → Certifications → Preferences), Agents opens on a five-stage pipeline view with the old detail behind **Advanced**, and loading states are skeletons; (6) two full end-to-end runs (a fresher persona and an internship persona) in `tests/test_pipeline_e2e.py`: onboard → 5 ranked jobs → 5 tailored resumes → Assurance keep/remove → applied → a chat change that lands in the database and the dashboard → provider switched to Kimi. One real bug found by that test and fixed: tailoring used to crash on a draft whose one-page fit had cut the third project bullet; the macro is now restored when new content needs it. **355 backend tests, 31 frontend tests pass**; `docs/API.md` and both AGENTS.md files describe the new behavior.
- **Two dashboards live on this Mac.** Annie's opens at **http://127.0.0.1:8010** (tab title *Annie · Career Workspace*). Port 8000 is a different person's dashboard (*Chetan · Career Workspace*): nothing there is hers, and her launcher now refuses to open a browser on it. If a tab ever shows someone else's jobs, check the address bar: it must say **8010**.
- **Port complete (18 Sep).** The workspace runs the newer career-dashboard app (six tabs, agents, Resume Studio), rebuilt around Annie: US only, the sponsorship gate, never-re-apply rules, one-page US Letter resumes, a signature project per company, and study plans. The old `system/`, `dashboard/`, `output/`, root docs and agent files are in `backup/2026-09-18-pre-port/`.
- **Fresh start (18 Sep).** The earlier database (USC and Databricks rows, old folders) was snapshotted to `backup/2026-09-18-fresh-start/` and cleared. Two real US postings were then found through the tracked career pages (`data/config/portals.yml`), gated and prepared:

  | Employer — role | Tier | Signature project | Watch-outs |
  |---|---|---|---|
  | Snowflake — Software Engineer, Database Engineering (Menlo Park) | B (H-1B history) | Expense analytics (`PROJ-P06-EXPENSE`) | Company research found the same requisition marked **CLOSED** on Snowflake's own careers site on 18 Sep; the Ashby link could not be confirmed either way. Open the posting in a browser before applying. |
  | Snowflake — Software Engineer, Backend (Menlo Park) | B (H-1B history) | Expense analytics (`PROJ-P06-EXPENSE`) | Posting says 2+ years' software experience; research, match and study plan are in the folder. |

  Both resumes are one US Letter page (`data/output/applications/Annie_Manoharan_Snowflake_01`, `_02`), scored under `career-assessment-v3` (ATS readiness 95). Neither has been applied to.
- **Excluded postings:** 34, each with the sentence that triggered the sponsorship gate (Dashboard → *Excluded roles*).
- **Goals:** 30 applications a week, Monday to Saturday, from 18 Sep.
- **Open questions** (`career-dashboard/data/context/QUESTIONS-FOR-YOU.md`): Q3 publication details (title, venue, year, author position), whether the 94% was measured, whether she is still at InsOps, city (Q5), salary (Q6), the work-authorization line (Q10), LinkedIn (Q11).
- **Assistant agent (19 Sep).** The chat is the first tab and the default landing page of Annie's dashboard, and it is now an agent rather than a router (`backend/services/assistant.py`, tools in `backend/services/assistant_tools.py`, `frontend/src/features/Assistant.tsx`, routes under `/api/v2/assistant`). A pasted posting still runs without any model: read the fields → sponsorship gate + never-re-apply → open the draft → fit one US Letter page → score → PDF, each step shown live; so do the shortcuts (`find jobs`, `status`, `applied to X` recorded only after a *yes*, `open X`, `excluded`, `help`). Anything else goes to the `workspace_agent` specialist, which sees the workspace snapshot, the tool catalogue and the task so far and returns one decision per turn (call a tool, ask her, or reply; at most 14 turns). Forty tools wrap every feature — jobs and the gates, Resume Studio (build, status, plain-English edits, undo, project sync, cover letter), the runnable agents (research, resume advisor, build, independent review, study plan; wait for a run and read its result), Profile (search, proposals that wait for review, reconcile, the questions ledger), goals, synced mail, settings and the policy file. Seven of them (`update_job`, `remove_job`, `restore_excluded`, `apply_profile_change`, `reconcile_profile`, `set_goals`, `resolve_mail`) pause the loop for her explicit *yes*, and the question describes the change in her words. The Gmail sync is deliberately not a tool. The runtime is the strong tier from Settings; when its provider has no key here the loop moves to what is ready (Claude Code, then Codex, then a keyed provider) and says so; the specialist team can now run on Codex too (`backend/ai/codex.py`), so choosing Codex as the main AI moves both tiers. The page has a right-hand rail: *Runs on* (switchable), the live flow of agents behind the current task (nodes light up as steps run; a queued run stays lit until it finishes), what is running, and every registry agent with whether the chat reaches it (click one to pre-fill its request); the front page offers six starter requests. Verified live on a disposable copy with Claude Code · sonnet: a status question (1 tool, 10 s), a profile update through proposal → *yes* → apply → the agent's follow-up question → reconcile → *yes* (four turns, ~50 s of model time), and a resume question that came back with the document card and the scorer's real gap. 22 new tests in `tests/test_assistant_agent.py` plus the 12 in `tests/test_assistant.py`; 3 frontend tests for the rail's flow.
- **Chat sync and the phone layout (19 Sep, later).** Annie's first tries on 8010 showed two faults. (1) Every job search failed in seconds with *Agent could not finish. Check Codex sign-in…*: the discovery schema had left `excluded` optional (commit of 18 Sep) and Codex's strict structured output refuses a schema whose `required` list skips a property; the runner discarded Codex's stderr, so the real reason never surfaced. Fixed: the runner hands Codex the closed form of every schema (`strict_schema`: every object closed, every property required) while the app's own result check keeps `excluded` optional, and a failed `codex exec` now reports its last error line (a sign-in problem is named as such). Codex itself is signed in and answers. (2) The page said *Runs on Claude Code · sonnet* while the runs went to Codex: with no tier chosen, the chat defaulted to OpenRouter (no key) and moved to Claude Code, while the gateway's main was Codex. Now an unchosen tier follows the main choice, `engine.runs` says where runs go, and a run the chat starts is enqueued on the chat's own engine when it can do the work — one *Runs on* for everything. On the client: a sent message is echoed at once and the poll restarts fast (it used to wait up to 8 s, which is why nine *help* messages went out in six seconds), a slow poll can no longer overwrite a newer one, a hung poll times out at 20 s, the thread only follows new activity when she is at the end (else a *New activity* pill), yes/no and pick-a-posting questions get one-tap answers in the composer, finished steps fold into one line, a failed reply has *Try again*, and rail clicks pre-fill instead of sending. Phones (≤640 px) get a bottom tab bar, a full-width thread and composer, and the agents drawer over the bar; the intro paragraph hides. Verified on a disposable copy over the DevTools protocol at 1440, 820 and 390 px, including a live question on Claude Code · sonnet (7 s), a pick-a-posting → yes/no → *no* sequence through the chips, and a failed exchange. 4 more backend tests, 2 more frontend tests.
  **Unresolved:** the server on 8010 runs the backend from before these fixes, so restart it (Ctrl+C in its Terminal, then double-click **Start Dashboard.command**) before the next search. Uncommitted, with the 19 Sep fixes.
- **Cost evaluation (19 Sep).** `career-dashboard/docs/COST-EVALUATION.md` estimates tokens and cost for one find-jobs-to-resume run on Claude Code, Codex, Gemini, Kimi and OpenRouter, and recommends an OpenRouter model-per-agent map (Opus 5 writes, a cross-family judge, Gemini 3.7 Flash extracts). Estimates only: `ai_calls.input_tokens/output_tokens` are still NULL; wiring `AgentTeam.on_usage` is the step that makes them real.
- **Chat rebuilt like a real chat app (20 Sep).** The Assistant tab now has what Annie expects from ChatGPT or Claude. **New chat** starts a fresh thread and the old one stays under **History** (a drawer listing every chat by day, with open, delete and **Clear chat history** for all of them at once, each behind a confirm; deleting removes only the messages — the jobs, resumes and profile changes a chat made stay); **Search** filters the open thread; **Export** downloads it as a Markdown file; **Stop** ends a reply in progress at its next step and a model decision that comes back after Stop is dropped, so nothing runs behind her back; every turn has copy, edit-and-resend and ask-again; day dividers, an avatar, typing dots with the current step, a scroll-to-latest pill, code blocks in replies, and keyboard shortcuts (Enter, Shift+Enter, ⌘⇧O new chat, ⌘K search, / to type, Esc). Server side, messages now belong to conversations (`assistant_messages.conversation_id`, added on first start; earlier messages join the open thread) and the agent's memory of recent exchanges is scoped to the open one (`GET/POST/DELETE /api/v2/assistant/conversations`, `PUT/DELETE /api/v2/assistant/conversations/{id}`, `POST /api/v2/assistant/messages/{id}/stop`; `docs/API.md`). The layout was checked on a disposable copy at 1440, 820 and 390 px: the toolbar goes icon-only under 900 px and the composer sits above the phone's bottom tab bar. 7 new backend tests in `tests/test_assistant.py` (331 pass), 12 frontend tests in `Assistant.test.tsx` (23 pass). **Uncommitted.**
- **Gmail:** not connected. Sync needs Codex signed in on this Mac; optional. The `email` agent must not be run from a shared sign-in.

## Next action

Restart the dashboard once (close its window, double-click **Start Dashboard.cmd**) so 8010 runs the current code; it then re-scores the saved jobs by the requirement check. Annie is at http://127.0.0.1:8010/p/annie/. For Annie: open **Resume Studio → Color Health → Check match** to see the requirement check, then **Assurance** to keep or remove its suggestions before applying through the posting link. The 07:00 run now runs the same engine as Daily Search (company list, then the web mix, every helper on). To add Srikanth: profile menu → **New profile** → attach `example_profile/About Me-1.docx` in the chat → **Read them now** → answer the questions → **Build my workspace**.

## Checks

`./Check Workspace.command` runs the backend tests, the workspace and layout validators, the React build and the component tests.

## Windows setup (20 Sep 2026)

- Installed Python 3.12, all backend and frontend dependencies, Tectonic and Poppler; added `Start Dashboard.cmd` for Windows.
- Added Windows support for timezone data, UTF-8 profile files, atomic batch writes, newline-stable artifact hashes, Codex `.cmd` launchers and portable tests.
- Verification passes: 318 backend tests, 16 frontend tests, Python dependency checks, frontend production build, zero npm audit findings, workspace/layout validation and the visually reviewed one-page base resume.
- The current database in this clone reports 0 jobs and 0 excluded roles; no application history was restored or changed during setup.
